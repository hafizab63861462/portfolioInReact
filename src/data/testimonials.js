// PURE DATA ONLY — see the header note in profile.js.
//
// Real testimonials only. While this array is empty, the knowledge-base
// builder emits a truthful "none published yet" record so the assistant
// answers review questions from grounded text instead of inventing praise.
// Adding real entries removes that record automatically — no code change.
//
// Shape: { id, author, role, company, text, projectSlug?, source? }

export const testimonials = [
  // ─────────────────────────────────────────────────────────────────────
  // PASTE REAL TESTIMONIALS HERE.
  // Do NOT add placeholder, sample or illustrative entries — the assistant
  // will quote whatever is in this file as genuine client feedback.
  // ─────────────────────────────────────────────────────────────────────
];
