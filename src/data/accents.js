// Split out of projectsData.js so client components can import the palette
// without dragging the ~51KB project prose into the browser bundle.
export const ACCENT = {
  blue: { color: "#2CBCE9", textDark: "#010026" },
  red: { color: "#DC4492", textDark: "#ffffff" },
  yellow: { color: "#FDCC49", textDark: "#010026" },
};
